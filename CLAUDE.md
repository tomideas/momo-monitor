# UI 设计规范

涉及 UI、主题、组件或新增页面的任务，先读取 `design-system/AI.md` 和最新 `design-system/design/design-system.json`。
使用用户已保存的设计；按真实源码 mapping 应用，保留未映射参数的限制。公共组件或主题变化时同步维护设计系统。
本项目为 WPF；验证源码、构建及相关原生界面后才报告已应用。不要用重新扫描覆盖用户尚未应用的设计。

修改产品 UI 后，同一任务回写涉及的共享设计；DESIGN.md 为自动生成文件，不独立修改。编辑器 App 自身的界面样式不应当作 Momo 产品设计回写。

